node_modules/
.next/
out/

# Derived from content/ at build time: the corpus the reading journey fetches
# a shard at a time. Rebuilt by `npm run corpus:shards`.
public/corpus/

.DS_Store
*.log
tsconfig.tsbuildinfo

# The source ebook is a commercial edition and is not redistributed here.
# Only the derived content/ JSON is committed; re-run `npm run etl` to rebuild it.
source/
*.epub
